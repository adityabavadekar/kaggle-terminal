# Shared secret password to authenticate requests to your relay server
RELAY_SECRET=change_this_to_your_random_secret_string

# Optional: PostgreSQL connection string
# If not set, the server falls back to an in-memory dictionary
DATABASE_URL=postgresql://user:password@localhost:5432/relay_db

# Port for local development (default: 5000)
PORT=5000
